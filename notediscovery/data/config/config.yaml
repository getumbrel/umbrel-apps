app:
  name: "NoteDiscovery"
  tagline: "Your Self-Hosted Knowledge Base"

server:
  host: "0.0.0.0"
  port: 8000
  reload: false
  allowed_origins: ["*"]
  debug: false

storage:
  notes_dir: "./data"
  plugins_dir: "./plugins"

search:
  enabled: true
