version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: vert_web_1
      APP_PORT: 80

  web:
    image: ghcr.io/vert-sh/vert:sha-438f91a@sha256:c604d357e44dce6020391b6bd8d20e4f5d35e3778ecf58bbccbd8fdc6792eaf4
    restart: on-failure
    environment:
      PUB_HOSTNAME: http://${DEVICE_DOMAIN_NAME}:3884
      PUB_VERTD_URL: http://${DEVICE_DOMAIN_NAME}:3884
      PUB_ENV: production
