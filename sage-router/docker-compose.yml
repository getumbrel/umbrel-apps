version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: sage-router_app_1
      APP_PORT: 8790

  app:
    image: ghcr.io/earlvanze/sage-router-public:v3.28.0@sha256:67d242ceec27fb81adfee0acb4d8e77cd42d61541f5d62c7a156bb91c50c424a
    restart: on-failure
    # not working rootless yet — needs /root/.openclaw and /root/.dario mounts
    #user: "1000:1000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ${APP_DATA_DIR}/data/config:/config:ro
      - ${UMBREL_ROOT}/.openclaw:/root/.openclaw:ro
      - ${UMBREL_ROOT}/.dario:/root/.dario
    environment:
      PORT: "8790"
      SAGE_ROUTER_HOME: /config
      SAGE_ROUTER_API_KEY: ${APP_SAGE_ROUTER_API_KEY:-local}
      SAGE_ROUTER_DISABLED_PROVIDERS: >-
        ollama-cloud,anthropic,dario,openrouter,nvidia-nim,darkbloom,ollama-cyber
      SAGE_ROUTER_DISABLED_MODELS: kimi-k2-thinking
      SAGE_ROUTER_PROFILE_OVERLAYS: ""
      SAGE_ROUTER_OLLAMA_TIMEOUT_SECONDS: "300"
      SAGE_ROUTER_BUNDLED_OLLAMA_AUTOSTART: "0"
      SAGE_ROUTER_OLLAMA_CLOUD_CATALOG_ENABLED: "0"
      SAGE_ROUTER_OLLAMA_AUTO_PULL_PATTERNS: ""
      OLLAMA_HOST: "http://host.docker.internal:11434"
      SAGE_ROUTER_OLLAMA_BASE_URL: "http://host.docker.internal:11434"
      OPENAI_CODEX_API_KEY: ${APP_SAGE_ROUTER_OPENAI_CODEX_API_KEY:-}
      SAGE_ROUTER_OPENAI_CODEX_AUTH_PROFILE_PATH: /root/.openclaw/agents/main/agent/auth-profiles.json
      SAGE_ROUTER_SHOW_MODEL_PREFIX: "1"
      SAGE_ROUTER_ALLOW_RESTART: "1"
