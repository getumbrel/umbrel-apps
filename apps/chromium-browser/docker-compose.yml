services:
  chromium:
    image: lscr.io/linuxserver/chromium:stable
    container_name: chromium
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
    volumes:
      - ${APP_DATA_DIR}/data:/config
    ports:
      - 3000:3000
    shm_size: "1gb"
    restart: on-failure
