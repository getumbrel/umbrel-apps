# ChainForensics Backend Dependencies
# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Async HTTP Client
httpx==0.26.0

# Database
sqlalchemy[asyncio]==2.0.25
aiosqlite==0.19.0

# Utilities
python-multipart==0.0.6
python-dotenv==1.0.0

# Note: Development dependencies (pytest) moved to requirements-dev.txt
# Note: Graph Analytics (igraph) removed - endpoints not used by HTML frontend
# Note: MCP removed from backend (only used in separate mcp/server.py container)
