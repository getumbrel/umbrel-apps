version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: slink_web_1
      APP_PORT: 3000

  web:
    image: anirdev/slink:v1.9.4@sha256:5f029012760b08c39cc08d034d78a5eae3bd413374f9955dc63ab9082602c081
    user: "1000:1000"
    restart: on-failure
    environment:
      TZ: UTC
      ORIGIN: http://${DEVICE_DOMAIN_NAME}:8811
      REQUIRE_SSL: "false"
      USER_APPROVAL_REQUIRED: "false"
      USER_PASSWORD_MIN_LENGTH: 8
      USER_PASSWORD_REQUIREMENTS: 15
      IMAGE_MAX_SIZE: 15M
      IMAGE_STRIP_EXIF_METADATA: "true"
      IMAGE_COMPRESSION_QUALITY: 80
      STORAGE_PROVIDER: local
    volumes:
      - ${APP_DATA_DIR}/data/var:/app/var/data
      - ${APP_DATA_DIR}/data/images:/app/slink/images
