version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: drivenest_app_1
      APP_PORT: 8478

  app:
    image: ghcr.io/earlvanze/drivenest:v0.2.0@sha256:47a845546fc77360c5f3780bb0a8b0163050ad08b361af7a616e47e385ae06cf
    restart: on-failure
    user: "1000:1000"
    init: true
    stop_grace_period: 30s
    volumes:
      - ${APP_DATA_DIR}/data:/data
    environment:
      - GDC_CONFIG=/data/config.json
      - GDC_DATA=/data
      - GDC_PORT=8478
      - GDC_RCLONE=/usr/local/bin/rclone
      - GDC_ROOT=/data/sync
      - RCLONE_CONFIG=/data/rclone/rclone.conf
      - PYTHONDONTWRITEBYTECODE=1
