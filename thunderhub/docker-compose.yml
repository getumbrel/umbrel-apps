version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: $APP_THUNDERHUB_IP
      APP_PORT: $APP_THUNDERHUB_PORT

  web:
    image: apotdevin/thunderhub:v0.13.13@sha256:c6041266090a527093ae51082620e1f3c51172f52114c31963b657183799457f
    restart: on-failure
    stop_grace_period: 1m
    volumes:
      - ${APP_LIGHTNING_NODE_DATA_DIR}:/lnd:ro
      - ${APP_DATA_DIR}/data:/data
    entrypoint: /data/entrypoint.sh
    environment:
      APP_PASSWORD: "$APP_PASSWORD"
      NO_VERSION_CHECK: "true"
      LOG_LEVEL: "debug"
      ACCOUNT_CONFIG_PATH: "/data/thubConfig.yaml"
      YML_ENV_1: "$APP_LIGHTNING_NODE_IP:$APP_LIGHTNING_NODE_GRPC_PORT"
      TOR_PROXY_SERVER: "socks://$TOR_PROXY_IP:$TOR_PROXY_PORT"
    networks:
      default:
        ipv4_address: $APP_THUNDERHUB_IP
