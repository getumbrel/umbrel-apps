version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: route96_server_1
      APP_PORT: 8001
      PROXY_AUTH_ADD: "false"

  server:
    image: voidic/route96:v0.6.0@sha256:03ddbef30329e9a0985f7109ff0d199e69135b416f17d44f8a83b7f93680322e
    user: "1000:1000"
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data/uploads:/app/data
      - ${APP_DATA_DIR}/data/config.yaml:/app/config.yaml
    environment:
      RUST_LOG: "info"
    depends_on:
      - db

  db:
    image: mariadb:11.2.4@sha256:e9ea1dbab6783ff0281e41e3357d950a5f598b2033559b8443e03b181807910b
    user: "1000:1000"
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data/db:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: "route96"
      MYSQL_USER: "route96"
      MYSQL_PASSWORD: "route96password"
      MYSQL_DATABASE: "route96"
