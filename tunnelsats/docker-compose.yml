version: "3.7"

services:
  tunnelsats:
    # Use the specific version tag and pinned digest for production (Grep ID 3033284912)
    image: tunnelsats/ts-umbrel-app:v3.1.0@sha256:c38f7bd28742b5e94a29351bcd44328d4080e742f5b904676b5008dc847f8f6a
    container_name: tunnelsats
    restart: on-failure
    network_mode: "host"
    cap_add:
      - NET_ADMIN
      - NET_RAW
    security_opt:
      - apparmor:unconfined
    volumes:
      # Persistent peer directory (Uninstall-safe)
      - ${APP_DATA_DIR}/../tunnelsats-data:/data
      # Lightning Node config mounts (Graceful discovery via relative paths)
      # These use relative paths to avoid the strict Umbrel "AND" dependency requirement
      - ${APP_DATA_DIR:-./data}/../lightning/data/lnd:/lightning-data/lnd
      - ${APP_DATA_DIR:-./data}/../core-lightning/data/lightningd/bitcoin:/lightning-data/cln
      # Host socket (For container detection & Docker API access)
      - /var/run/docker.sock:/var/run/docker.sock:ro
