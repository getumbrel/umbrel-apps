name: Lint apps

on:
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  lint-apps:
    name: Lint apps
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
      - uses: sharknoon/umbrel-app-linter-action@v1.1.8
