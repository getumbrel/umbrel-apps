name: Lint apps

on:
  pull_request_target:
    branches:
      - master

jobs:
  lint-apps:
    name: Lint apps
    runs-on: ubuntu-latest
    steps:
      - uses: sharknoon/umbrel-app-linter-action@3d349ae674b5c558c4605ccf75cdce6c5964a48a