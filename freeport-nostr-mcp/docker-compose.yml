version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: freeport-nostr-mcp_server_1
      APP_PORT: 8788

  server:
    image: ghcr.io/ptrinh/freeport-nostr-mcp:0.6.1@sha256:09eb3455ed5e5e92bddb3a7cc05bb192df0cf596f91318475c068f7e43d79b01
    restart: on-failure
    stop_grace_period: 1m
    environment:
      - HOST=0.0.0.0
      - PORT=8788
      # Rate limits (per-IP + global token buckets)
      - RATE_LIMIT_PER_MIN=300
      - RATE_LIMIT_BURST=60
      - RATE_LIMIT_GLOBAL_PER_MIN=2500
      # Push notifier. VAPID keys auto-generate into DATA_DIR on first run.
      - ENABLE_NOTIFY=1
      - DATA_DIR=/data
      - VAPID_SUBJECT=mailto:admin@umbrel.local
    volumes:
      - ${APP_DATA_DIR}/data:/data
