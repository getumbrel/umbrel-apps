services:
  app_proxy:
    environment:
      APP_HOST: sv2-ui_sv2-ui_1
      APP_PORT: 8080
      PROXY_AUTH_ADD: "false"
    depends_on:
      - sv2-ui

  docker:
    image: docker:27.2.0-dind@sha256:f9f72ad901a78f27be922b2d320bbc263174f12919c1b37e6a01f828fa904565
    privileged: true
    network_mode: host
    stop_grace_period: 1m
    restart: on-failure
    environment:
      DOCKER_ENSURE_BRIDGE: "dind0:10.32.0.1/16"
    entrypoint: /entrypoint.sh
    command: >
      dockerd
        --bridge dind0
        --data-root /data/data
        --exec-root /data/exec
        --host unix:///data/docker.sock
        --pidfile /data/docker.pid
    healthcheck:
      test: ["CMD", "docker", "-H", "unix:///data/docker.sock", "info"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    volumes:
      - ${APP_DATA_DIR}/entrypoint.sh:/entrypoint.sh
      - ${APP_DATA_DIR}/data/docker:/data
      - ${APP_BITCOIN_DATA_DIR}:/root/.bitcoin:ro
      # Mount setup config into DIND so nested containers can access it.
      - ${APP_DATA_DIR}/data/config:/app/data/config

  sv2-ui:
    image: stratumv2/sv2-ui:v0.3.0@sha256:dc6654c4f5ff985d0af2533ee01a638573085e3154c0710a8c69000e7ee087f6
    pull_policy: always
    stop_grace_period: 1m
    restart: on-failure
    # required: sv2-ui expects docker DNS to resolve container names in production.
    # With dind, the inner network (sv2-network) isn't directly accessible from outer network.
    # since dind is network_mode: host we just map it to host-gateway
    extra_hosts:
      - "sv2-translator:host-gateway"
      - "sv2-jdc:host-gateway"
    environment:
      HOST_OS: "umbrel"
    volumes:
      - ${APP_DATA_DIR}/data/docker:/var/run
      # Config files written by sv2-ui after setup.
      - ${APP_DATA_DIR}/data/config:/app/data/config
    depends_on:
      docker:
        condition: service_healthy
