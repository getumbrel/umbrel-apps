{
  "name": "codex-agent",
  "version": "0.1.1",
  "private": true,
  "description": "Private Umbrel UI gateway for Codex app-server",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
