{
  "name": "codex-agent",
  "version": "0.3.0",
  "private": true,
  "description": "Private OpenAI-compatible Umbrel bridge for Codex app-server",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
