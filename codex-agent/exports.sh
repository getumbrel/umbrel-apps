# Codex Agent is configured from either chat client's own settings, so it does
# not export credentials to or modify another app. Its internal bridge key is
# supplied directly by Compose from Umbrel's per-install APP_PASSWORD.
