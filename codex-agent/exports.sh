# Codex Agent is a client dependency, so it does not export credentials to
# another app. Its internal bridge key is supplied directly by Compose from
# Umbrel's per-install APP_PASSWORD after dependency exports are sourced.
