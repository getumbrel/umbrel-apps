services:
  navidrome:
    image: deluan/navidrome:latest
    restart: on-failure
    environment:
      - PUID=1000
      - PGID=1000
    ports:
      - "4533:4533"
    volumes:
      - ${APP_DATA_DIR}/data:/data
      - ${UMBREL_ROOT}/data/storage/music:/music:rw
    container_name: navidrome_1
