manifestVersion: 1
id: btcpay-server
category: bitcoin
name: BTCPay Server
version: "2.4.4"
tagline: Accept Bitcoin payments with 0 fees & no 3rd party
description: >-
  BTCPay Server is a payment processor that allows you to receive
  payments in Bitcoin (and altcoins) directly, with no fees, transaction cost or
  a middleman. It is a non-custodial invoicing system which eliminates the
  involvement of a third-party.


  Payments with BTCPay Server go directly to your wallet, which increases the privacy and security. Your private keys are never uploaded to your Umbrel. There is no address re-use, since each invoice generates a new address deriving from your xpubkey.


  You can not only attach an unlimited number of stores and use the Lightning Network but also become a payment processor for others. Thanks to the apps built on top of it, you can use BTCPay to receive donations, start a crowdfunding campaign or have an in-store Point of Sale.


  Please note: Due to your BTCPay instance running on your local network connecting remote applications, such as Shopify or WordPress for example, will fail to connect, unless you configure the dynamic DNS feature (see: https://docs.btcpayserver.org/Deployment/DynamicDNS).
developer: BTCPay Server Foundation
website: https://btcpayserver.org
dependencies:
  - bitcoin
  - lightning
repo: https://github.com/btcpayserver/btcpayserver
support: https://chat.btcpayserver.org
port: 3003
gallery:
  - 1.jpg
  - 2.jpg
  - 3.jpg
path: ""
defaultUsername: ""
defaultPassword: ""
releaseNotes: |-
  ⚠️ Security reminder for skipped-version updates. If you used BTCPay Server before 2.4.2 and have not rotated your Lightning Node credentials since updating to 2.4.2 or later, update first, then complete every step below immediately. Updating alone does not invalidate credentials that may have been exposed.

  After updating, rotate your Lightning Node credentials:

  1. On the umbrelOS home screen, right-click BTCPay Server and select **Stop**.
  2. Right-click Lightning Node and select **Stop**. Wait until both apps have stopped before continuing.
  3. Open Files and go to `Apps` → `lightning` → `data` → `lnd` → `data` → `chain` → `bitcoin` → `mainnet`.
  4. In that folder, delete:

     - Every file whose name ends in `.macaroon`
     - `macaroons.db`

     The exact `.macaroon` filenames vary between installations. It is normal to have more or fewer of them. Delete all files ending in `.macaroon`.

     ⚠️ Do not delete anything else. Keep `wallet.db`, `channel.backup`, the `chan-backup-archives` folder, and every other file or folder. Never delete `channel.db`.

  5. Return to the umbrelOS home screen and click Lightning Node to start it. Wait for it to finish starting.
  6. Return to the same folder and confirm that new `admin.macaroon` and `macaroons.db` files have appeared.
  7. Click BTCPay Server to start it.
  8. Right-click and restart any other Umbrel apps connected to Lightning Node.
  9. Reconnect external Lightning wallets and services. Their old credentials will no longer work.

  Check your Lightning payments, channel closures, peers, on-chain balance, and channel balance for anything you do not recognize.

  Prior security advisory: https://x.com/BtcpayServer/status/2085865561137831938

  This release changes several existing workflows:

  - Legacy BitPay Basic-auth API keys are removed. Update integrations that use them before upgrading.
  - Greenfield API clients must use the API key's ID, rather than its secret, when revoking a key by identifier. Existing Greenfield API keys continue to work for authentication.
  - Point of Sale invoices no longer accept a per-request `notificationUrl`. Configure the notification URL in the Point of Sale app instead.
  - NFC checkout payments are disabled by default, a change introduced in 2.4.3. Enable them under Store Settings → Checkout Experience if needed.
  - Zero-amount invoices are blocked unless enabled in the store's settings, and invited users must accept before joining a store.
  - Desktop smartcard setup for Boltcards has been replaced by the Boltcard app.

  This release also improves invoice permissions, exchange-rate rules, support-link security, account-email changes, and plugin discovery. API keys are now stored as hashes, and NBXplorer is updated to 2.6.13.

  Full release notes: https://github.com/btcpayserver/btcpayserver/releases/tag/v2.4.4
submitter: Umbrel
submission: https://github.com/getumbrel/umbrel/pull/353
