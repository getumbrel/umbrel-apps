version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: actual-budget-web-1
      APP_PORT: 5006
      PROXY_AUTH_ADD: "false"
  
  web:
    image: actualbudget/actual-server:latest
    restart: on-failure
    stop_grace_period: 1m
    volumes:
      - ${APP_DATA_DIR}/data:/data