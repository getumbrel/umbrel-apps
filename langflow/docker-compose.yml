version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: langflow_app_1
      APP_PORT: 7860

  app:
    image: langflowai/langflow:1.11.4@sha256:3fe9a758b8a4fa9bbeec726d71d2f69391643bfc20c175288c1b4e166b7348fb
    user: "1000:1000"
    restart: on-failure
    environment:
      LANGFLOW_CONFIG_DIR: /app/langflow
      HOME: /app/langflow
      DO_NOT_TRACK: "true"
      LANGFLOW_AUTO_LOGIN: "true"
      LANGFLOW_SUPERUSER_PASSWORD: ${APP_PASSWORD}
      LANGFLOW_DATABASE_URL: "postgresql://langflow:langflow@langflow_db_1:5432/langflow"
    volumes:
      - ${APP_DATA_DIR}/data/app:/app/langflow

  db:
    image: postgres:16.14-trixie@sha256:4b7183ac05f8ef417db21fd72d71047a4238340c261d3cc3ddb6d579ab5071ae
    user: "1000:1000"
    restart: on-failure
    environment:
      POSTGRES_USER: langflow
      POSTGRES_PASSWORD: langflow
      POSTGRES_DB: langflow
    volumes:
      - ${APP_DATA_DIR}/data/db:/var/lib/postgresql/data
