version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: downtify_downtify_1
      APP_PORT: 8000

  downtify:
    image: henriquesebastiao/downtify:0.2.3@sha256:0bf0f38cbf44dc849c890ab7eee2b601d9fcb72136c8cf7388fc6a1080c804a8
    restart: on-failure
    stop_grace_period: 1m
    volumes:
      - ${UMBREL_ROOT}/data/storage/downloads/downtify:/downloads
