version: "3.7"
services:
  app:
    image: sparkielabs/lightning-control-center:latest
    restart: on-failure
    ports:
      - "8765:8765"
    environment:
      - RPC_USER=${APP_BITCOIN_RPC_USER}
      - RPC_PASS=${APP_BITCOIN_RPC_PASS}
      - RPC_HOST=${APP_BITCOIN_RPC_HOST}
      - RPC_PORT=${APP_BITCOIN_RPC_PORT}
    volumes:
      - ${APP_DATA_DIR}/data:/app/data
