version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: amato_web_1
      APP_PORT: 80
      
  web:
    image: berkeleyengineer/mopomo:v0.0.1
    restart: on-failure
    stop_grace_period: 1m
    environment:
      PORT: 80