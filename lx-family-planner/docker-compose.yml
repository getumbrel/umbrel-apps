version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: lx-family-planner_server_1
      APP_PORT: 3001
      PROXY_AUTH_ADD: "false"

  server:
    image: ghcr.io/laxxx-lab/lx-family-planner:1.14.0@sha256:952301dffc9d5a5a9c85660f66c7f415f0b019acfece7c9694ca8ba6b9e3a551
    user: "1000:1000"
    restart: on-failure
    stop_grace_period: 15s
    environment:
      NODE_ENV: production
      PORT: 3001
      TZ: Europe/Berlin
      APP_SECRET: "${APP_SEED}"
      SESSION_COOKIE_SECURE: auto
      DATABASE_FILE: /app/data/family_planner.sqlite
      LEGACY_DATABASE_FILE: /app/data/family_db.json
    volumes:
      - "${APP_DATA_DIR}/data:/app/data"
      - "${APP_DATA_DIR}/backups:/app/backups"
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
